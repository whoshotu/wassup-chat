version: '3.8'
services:
  scaffolder:
    build: .
    ports:
      - "4000:4000"
    environment:
      - LIBRETRANSLATE_URL=http://libretranslate:5000
  libretranslate:
    image: libretranslate/libretranslate:latest
    ports:
      - "5000:5000"
